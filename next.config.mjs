/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'th.bing.com',
        'img.freepik.com',
        // Add other domains you need here
      ],
    },
  };

export default nextConfig;
